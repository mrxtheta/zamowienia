<div class="hero-unit">

<h1>Towary</h1>


<div style="test-align:center;">

<%= link_to("Dodaj nowe towar", {:action => 'nowy'}, :class => 'btn btn-mini btn-primary ')%>

</div>

<br>

<table class="table table-striped">
	<tr class="info">
		<th>Nazwa</th>
		<th>Cena</th>
		<th>Opis</th>
		<th>Data dodania</th>

		</tr>

<% @towary.each do |tow| %>


	<tr>
		<td><%= tow.nazwa %> </td>
		<td><%= tow.cena %> </td>
		<td><%= tow.opis %> </td>
		<td><%= tow.created_at %> </td>
		
		

		<td class="actions">

<%= link_to("Pokaz", {:action => 'pokaz', :id => tow.id}, :class => 'btn btn-xs btn-default') %>
<%= link_to("Edycja", {:action => 'edycja', :id => tow.id}, :class => 'btn btn-xs btn-primary') %>
<%= link_to("Usun", {:action => 'usun', :id => tow.id}, :class => 'btn btn-xs btn-danger') %>

</th>
</tr>
	<% end %>
	</table>